{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the jakal-flow desktop shell",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
